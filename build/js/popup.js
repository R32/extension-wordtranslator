// Generated by Haxe 5.0.0-alpha.1+765abfa
(function ($global) { "use strict";
function NOP(_) {
}
function childpos(elem) {
	let index = 0;
	let prev = elem.previousSibling;
	while(prev) {
		if(prev.nodeType == 1) {
			++index;
		}
		prev = prev.previousSibling;
	}
	return index;
}
function set_redirect(enable) {
	let obj = { };
	obj[enable ? "enableRulesetIds" : "disableRulesetIds"] = ["redirect-googleapi"];
	chrome.declarativeNetRequest.updateEnabledRulesets(obj).catch(NOP);
}
function update(label,checked,extra) {
	if(checked) {
		label.setAttribute("checked","");
	} else {
		label.removeAttribute("checked");
	}
	let menu = document.querySelector("#menumain");
	let ui_redirect = menu.children[1];
	let ui_sound = menu.children[2];
	let ui_speed = menu.children[3];
	switch(childpos(label)) {
	case 0:
		let disabled = !checked;
		attrset(ui_speed,"disabled",disabled);
		attrset(ui_sound,"disabled",disabled);
		attrset(ui_redirect,"disabled",disabled);
		chrome.storage.local.set({"disabled" : disabled},function() {
			chrome.runtime.sendMessage([2,"disabled" + ":" + (disabled == null ? "null" : "" + disabled)]).catch(NOP);
		});
		if(disabled) {
			set_redirect(false);
		} else {
			chrome.storage.local.get(["redirect"],function(res) {
				set_redirect(res["redirect"]);
			});
		}
		break;
	case 1:
		chrome.storage.local.set({"redirect" : checked},function() {
			set_redirect(checked);
		});
		break;
	case 2:case 3:
		let key = label == ui_sound ? "voices" : "vspeed";
		let obj = { };
		obj[key] = extra;
		chrome.storage.local.set(obj,function() {
			chrome.runtime.sendMessage([2,key + ":" + extra]).catch(NOP);
		});
		break;
	default:
	}
}
function attrset(label,value,enable) {
	if(enable) {
		label.setAttribute(value,"");
		label.querySelector("input").setAttribute(value,"");
	} else {
		label.removeAttribute(value);
		label.querySelector("input").removeAttribute(value);
	}
}
function mapspan(k) {
	let v = ({ "1" : "2", "2" : "4", "3" : "8", "4" : "inf"})[k];
	if(v == null) {
		return k;
	} else {
		return v;
	}
}
function main() {
	let menu = document.querySelector("#menumain");
	menu.onclick = function(e) {
		let label = e.target;
		let parent = label.parentElement;
		if(parent == menu && (label == menu.children[2] || label == menu.children[3])) {
			e.preventDefault();
			let input = label.querySelector("input");
			let checked = !label.hasAttribute("checked");
			let value = checked ? input.value : "" + ((input.value | 0) + 256);
			update(label,checked,value);
			label.querySelector("span").innerText = mapspan(input.value);
			return;
		}
		let input = label;
		if(input.type == "checkbox") {
			update(parent,input.checked);
		} else if(input.type == "range") {
			update(parent,true,input.value);
			parent.querySelector("span").innerText = mapspan(input.value);
		}
	};
	chrome.storage.local.get(["voices","vspeed","disabled","redirect"],function(res) {
		let menu = document.querySelector("#menumain");
		let ui_speed = menu.children[3];
		let ui_voices = menu.children[2];
		let ui_redirect = menu.children[1];
		if(res["disabled"]) {
			attrset(menu.children[0],"checked",false);
			attrset(ui_redirect,"disabled",true);
			attrset(ui_voices,"disabled",true);
			attrset(ui_speed,"disabled",true);
		}
		if(res["redirect"]) {
			attrset(ui_redirect,"checked",true);
			set_redirect(true);
		}
		let aui = [ui_voices,ui_speed];
		let akey = ["voices","vspeed"];
		let i = 0;
		while(i < aui.length) {
			let ui = aui[i];
			let svalue = res[akey[i++]];
			if(svalue == null) {
				continue;
			}
			let iv = (svalue | 0);
			if(iv > 255) {
				attrset(ui,"checked",false);
			}
			let svalue1 = "" + (iv & 255);
			ui.querySelector("input").value = svalue1;
			ui.querySelector("span").innerText = mapspan(svalue1);
		}
	});
}
{
}
main();
})(window);
